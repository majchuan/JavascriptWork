/**
 * @param {string[]} strs
 * @return {number}
 */
var findLUSlength = function(strs) {    
    const strsSet = new Set(strs);
    const strsArr = [...strsSet];

    strsArr.sort((x,y) => x.length - y.length);
    
};

const isSubsequence =(strA, strB) =>{
    
}

const strts =["aabbcc", "aabbcc","cb","abc"];

